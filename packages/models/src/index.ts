/**
 * @lazy-code/models — model provider layer.
 *
 * Phase 1 lands here: model ID parser (openai/…, openrouter/…, anthropic/…),
 * ModelRegistry (resolve + metadata), error normalization.
 */
export {};
